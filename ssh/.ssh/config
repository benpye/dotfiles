# keep connections open for 4 hours
ControlPersist		4h
ControlMaster		auto
ControlPath		~/.ssh/sockets/socket-%r@%h:%p

# allow host key verification via SSHFP records
VerifyHostKeyDNS	yes

# Make cwm's ssh menu work
HashKnownHosts		no
